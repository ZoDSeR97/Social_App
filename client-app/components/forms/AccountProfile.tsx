"use client"

interface Props {
    user: {};
    btnTitle: string;
}

export const AccountProfile = ({ user, btnTitle } : Props) => {
    return (
        <div>
            Account Profile
        </div>
    
    )
}